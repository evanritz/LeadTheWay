package org.evan.LeadTheWay.client;

public class hostname {
	
	public String hostname = null;
	
	public hostname(String hostname) { this.hostname = hostname; }
	
	public hostname() {}
	
	public void set(hostname passed_hostname) { hostname = passed_hostname.hostname; }
}
